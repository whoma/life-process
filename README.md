# Life Progress - 人生进度可视化 App

一款简洁优雅的iOS应用，帮助你可视化人生进度，通过格子日历和进度条两种形式呈现时间的流逝。

## ✨ 功能特性

### 主App功能
- 📅 **人生日历**: 每个格子代表一天，直观展示已过和未来的时间
- 📊 **多维度进度**: 显示今日/本周/本月/本年/人生的进度
- 🎨 **简洁设计**: 极简风格，专注于时间可视化
- ⚙️ **基础设置**: 设置出生日期和预期寿命

### Widget功能
- 📱 **小组件**: 显示人生总进度和百分比
- 📱 **中组件**: 显示最近一年的格子日历
- 📱 **大组件**: 显示完整的格子日历和统计信息

## 🏗️ 项目结构

```
LifeProgress/
├── LifeProgressApp.swift          # 应用入口
├── Models/                        # 数据模型
│   ├── UserLifeData.swift         # 用户数据（出生日期、预期寿命等）
│   ├── LifeProgress.swift         # 生命进度计算
│   └── Theme.swift                # 主题颜色系统
├── ViewModels/                    # 视图模型
│   └── LifeProgressViewModel.swift # 数据管理和业务逻辑
├── Views/                         # 视图
│   ├── Onboarding/
│   │   └── OnboardingView.swift   # 首次配置页面
│   ├── Main/
│   │   ├── MainTabView.swift      # 主标签页
│   │   ├── GridView.swift         # 格子日历视图
│   │   └── ProgressView.swift     # 进度条视图
│   └── Settings/
│       └── SettingsView.swift     # 设置页面
├── Utilities/                     # 工具类
│   ├── DateCalculator.swift       # 日期计算工具
│   ├── DataManager.swift          # 数据持久化管理
│   └── Constants.swift            # 常量定义
└── Widget/                        # Widget扩展
    ├── LifeProgressWidget.swift   # Widget入口
    ├── SmallWidgetView.swift      # 小组件视图
    ├── MediumWidgetView.swift     # 中组件视图
    └── LargeWidgetView.swift      # 大组件视图
```

## 🚀 本地开发指南

### 前置要求

- macOS 13.0 或更高版本
- Xcode 15.0 或更高版本
- iOS 16.0 或更高版本（目标设备）

### 步骤1: 创建Xcode项目

1. 打开Xcode，选择 **Create a new Xcode project**
2. 选择 **iOS → App**
3. 填写项目信息：
   - **Product Name**: `LifeProgress`
   - **Organization Identifier**: `com.yourname` (改成你自己的)
   - **Interface**: SwiftUI
   - **Language**: Swift
   - **Storage**: 不勾选 Core Data
4. 保存项目到本地目录

### 步骤2: 导入源代码

将本仓库的 `LifeProgress/` 文件夹中的所有 `.swift` 文件添加到Xcode项目中：

1. 在Xcode中右键项目根目录
2. 选择 **Add Files to "LifeProgress"...**
3. 选择所有源代码文件，确保勾选 **Copy items if needed**
4. 确保 **Target Membership** 中勾选了 `LifeProgress` target

### 步骤3: 配置App Group

App Group用于主App和Widget之间共享数据。

1. 选中项目根节点 → **Signing & Capabilities**
2. 点击 **+ Capability** → 添加 **App Groups**
3. 点击 **+** 按钮，创建App Group：
   ```
   group.com.yourname.lifeprogress
   ```
   > ⚠️ 注意：需要将 `yourname` 替换为你的标识符

4. 更新 `Constants.swift` 中的 App Group 标识符：
   ```swift
   static let appGroupIdentifier = "group.com.yourname.lifeprogress"
   ```

### 步骤4: 添加Widget Extension

1. 在Xcode菜单栏选择 **File → New → Target**
2. 选择 **iOS → Widget Extension**
3. 填写信息：
   - **Product Name**: `LifeProgressWidget`
   - **Include Configuration Intent**: 不勾选
4. 点击 **Activate** 激活scheme

5. 删除自动生成的 `LifeProgressWidget.swift` 文件

6. 添加Widget源代码：
   - 将 `Widget/` 目录下的所有文件添加到 `LifeProgressWidget` target
   - 在 **Target Membership** 中确保这些文件属于 `LifeProgressWidget` target

7. 共享代码到Widget target：
   - 选中Models、Utilities文件夹中的所有文件
   - 在右侧 **File Inspector** 的 **Target Membership** 中
   - 同时勾选 `LifeProgress` 和 `LifeProgressWidget`

8. 为Widget Extension配置App Group：
   - 选中 `LifeProgressWidget` target
   - 添加 **App Groups** capability
   - 添加相同的 `group.com.yourname.lifeprogress`

### 步骤5: 配置Info.plist

在主App的 `Info.plist` 中添加（如果需要）：

```xml
<key>NSUserTrackingUsageDescription</key>
<string>我们需要追踪使用情况以改善用户体验</string>
```

> 注：本应用不需要特殊权限

### 步骤6: 运行和测试

1. **运行主App**:
   - 选择 `LifeProgress` scheme
   - 选择模拟器或真机
   - 点击运行 ▶️

2. **运行Widget**:
   - 选择 `LifeProgressWidget` scheme
   - 运行后会弹出Widget选择界面
   - 选择对应的Widget大小进行测试

3. **测试Widget添加**:
   - 在主屏幕长按空白处
   - 点击左上角 **+** 按钮
   - 搜索 "Life Progress"
   - 添加不同大小的Widget

## 📱 使用说明

### 首次启动

1. 应用会显示欢迎页面
2. 选择你的出生日期
3. 点击"开始"按钮

### 主界面

#### 日历标签页
- 显示人生格子日历，每个格子代表一天
- 深色格子表示已过去的天数
- 浅色格子表示未来的天数
- 红圈标记表示今天
- 点击格子可以查看具体日期

#### 进度标签页
- 人生总进度（大号显示）
- 今日进度
- 本周进度
- 本月进度
- 本年进度

### 设置页面

在主界面任意位置下拉可以访问设置：
- 修改出生日期
- 查看统计信息
- 重置数据（用于测试）

## 🎨 主题系统

应用内置4种配色方案：

1. **经典蓝** (默认)
   - 已过: `#1E3A8A`
   - 未来: `#DBEAFE`

2. **暖阳橙**
   - 已过: `#C2410C`
   - 未来: `#FED7AA`

3. **森林绿**
   - 已过: `#065F46`
   - 未来: `#D1FAE5`

4. **极简黑白**
   - 已过: `#000000`
   - 未来: `#F3F4F6`

> 当前版本使用默认主题，后续版本会添加主题切换功能

## 🔧 技术栈

- **SwiftUI**: 现代声明式UI框架
- **WidgetKit**: iOS Widget开发
- **Combine**: 响应式编程
- **UserDefaults + App Group**: 数据持久化和共享

## 📝 数据存储

所有数据存储在本地，使用App Group共享：
- 用户出生日期
- 预期寿命
- 主题配置

**隐私保护**：
- 不上传任何数据到服务器
- 不收集用户信息
- 完全离线使用

## 🐛 常见问题

### Widget不显示数据？

1. 确保App Group配置正确
2. 确保主App和Widget使用相同的App Group ID
3. 在主App中重新设置出生日期，保存数据

### 格子显示不正确？

1. 检查出生日期是否设置正确
2. 尝试下拉刷新
3. 重启应用

### 编译错误？

1. 确保所有文件的Target Membership配置正确
2. Models和Utilities需要同时属于主App和Widget target
3. Views只属于主App target
4. Widget文件只属于Widget target

## 🚧 后续计划

- [ ] 主题切换功能
- [ ] 里程碑标记
- [ ] 导出分享图片
- [ ] iCloud数据同步
- [ ] iPad适配
- [ ] Apple Watch支持

## 📄 许可证

MIT License

## 👨‍💻 作者

开发日期: 2025-11-10

---

**享受你的每一天！⏳**
